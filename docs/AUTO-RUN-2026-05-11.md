# 自动执行任务 · 2026-05-11

> 由 Claude 一次会话自动执行。每完成一项打勾,每个 Phase 单独 commit。
> 启动前提:本地 docker-compose 已起、后端依赖已装、`/api/health` 可通。

## Phase D — 工作树收尾

- [x] D1. `.claude/` 加进 `.gitignore`
- [x] D2. Commit 已有的 `package.json` workspaces 变更 + `package-lock.json`
- [x] D3. **Commit 节点**:`chore: 工作树收尾`

## Phase B — 数据模型 + 首次迁移

设计 5 张核心表(MVP 最小集),用 TypeORM entity + migration。
所有表统一约定:主键 `id` CHAR(26) ULID、`created_at` / `updated_at` DATETIME(3) NOT NULL DEFAULT、charset utf8mb4_unicode_ci、引擎 InnoDB。

- [ ] B1. 在各业务 module 下建 entity:
  - [ ] `user.entity.ts` — User(openid 唯一、role/status、phone、nickname、avatar)
  - [ ] `pig.entity.ts` — Pig(商家发布的猪,total_shares / sold_shares / price_per_share)
  - [ ] `order.entity.ts` — Order(user_id, pig_id, shares_count, total_price, status, paid_at)
  - [ ] `share.entity.ts` — Share(订单成功后落账,user 持有的份额)
  - [ ] `wallet.entity.ts` — Wallet(user 一对一,balance / frozen)
- [ ] B2. 修 `database/data-source.ts`:让它从 `.env.development` 读凭据
- [ ] B3. `npm run migration:generate -- db/migrations/InitialSchema` 生成迁移
- [ ] B4. `npm run migration:run` 执行,Navicat 验证表存在
- [ ] B5. **Commit 节点**:`feat(backend): 5 张核心表 entity + 首个 migration`

## Phase C — Auth + JWT (含 dev-login 旁路)

- [ ] C1. `UserModule` — `UserService.findOrCreateByOpenid()` + `UserRepository`
- [ ] C2. `AuthModule`:
  - [ ] `JwtStrategy`(从 Bearer Token 提取 sub → 加载 User)
  - [ ] `JwtAuthGuard` 全局生效,`@Public()` 装饰器跳过
  - [ ] `AuthService.signTokens(user)` → access + refresh
- [ ] C3. `POST /api/auth/wx-login`(生产路径)
  - body: `{ code: string }`
  - 调用 WeChat `jscode2session`,拿 openid → findOrCreateByOpenid → 签发 JWT
  - 本地 WX_MP_APPID 是占位符,真实调用会失败,日志记录、返回 502
- [ ] C4. `POST /api/auth/dev-login`(旁路,**仅 NODE_ENV !== production**)
  - body: `{ openid?: string }` 默认 `dev_user_001`
  - 直接 findOrCreateByOpenid + 签 JWT
- [ ] C5. `GET /api/auth/me`(受保护,返回当前 user)用于验证 guard
- [ ] C6. curl 走一遍:dev-login → 拿 token → 调 /me 通过
- [ ] C7. **Commit 节点**:`feat(auth): JWT + 微信登录路径 + dev-login 旁路`

## Phase A — uni-app 前端骨架

- [ ] A1. `degit dcloudio/uni-preset-vue#vite-ts frontend/`(非交互模板克隆)
- [ ] A2. `frontend/` 加进根 `package.json` workspaces;装依赖
- [ ] A3. `frontend/src/utils/request.ts` — axios 封装,baseURL 走环境变量
- [ ] A4. `frontend/.env.development` — `VITE_API_BASE=http://127.0.0.1:3000/api`
- [ ] A5. 改首页 `pages/index/index.vue`:
  - 显示 `/api/health` 探活结果
  - 一个按钮:点击调 `/api/auth/dev-login` 拿 token、调 `/api/auth/me` 显示用户
- [ ] A6. `npm run dev:h5` 起 H5 dev server,验证浏览器打开能跑通
- [ ] A7. **Commit 节点**:`feat(frontend): uni-app 骨架 + 登录 demo + 健康检查页`

---

## 风险与回退

- TypeORM migration 生成依赖 DB 可连(已就绪)
- 微信真实登录在本地不会成功(占位 appid),靠 dev-login 验证整条链路
- uni-app 模板需要网络拉 GitHub,degit 走 HTTPS,失败时改用 `git clone`
- 任意一步失败:本 Phase 不 commit,记录失败原因 → 中止自动流程并报告

## 时间线

开始:_(填实际时间)_
